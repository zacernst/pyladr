% Group theory: left cancellation law
% Easy-medium difficulty, good for basic benchmarking

assign(max_seconds, 30).

formulas(sos).
  e * x = x.
  x' * x = e.
  (x * y) * z = x * (y * z).
end_of_list.

formulas(goals).
  x * y = x * z -> y = z.
end_of_list.
