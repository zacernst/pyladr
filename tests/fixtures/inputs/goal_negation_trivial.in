% Soundness canary: P(a) should NOT prove all x P(x).
% With correct Skolemization, goal P(x) becomes -P(c1) where c1 is fresh.
% -P(c1) does not resolve with P(a) since c1 != a.

formulas(sos).
P(a).
end_of_list.

formulas(goals).
P(x).
end_of_list.
