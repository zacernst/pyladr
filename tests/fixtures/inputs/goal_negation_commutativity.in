% Commutativity with goal negation.
% Tests that Skolemization is correctly applied in goal processing.

formulas(sos).
f(x,y) = f(y,x).
f(e,x) = x.
end_of_list.

formulas(goals).
f(a,b) = f(b,a).
end_of_list.
