% Lattice idempotence from absorption laws.
% meet(x, join(x, y)) = x and join(x, meet(x, y)) = x.

formulas(sos).
meet(x, join(x, y)) = x.
join(x, meet(x, y)) = x.
end_of_list.

formulas(goals).
meet(a, a) = a.
end_of_list.
