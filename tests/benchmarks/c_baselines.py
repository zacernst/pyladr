"""C Prover9 reference baselines for performance comparison.

These baselines are generated by running the C prover9 binary on benchmark
problems. They serve as the ground truth for behavioral equivalence testing
and performance target assessment.

Regenerate with: python -m tests.benchmarks.bench_harness --c-only --save
"""

from __future__ import annotations

from dataclasses import dataclass


@dataclass(frozen=True)
class CBaseline:
    """Reference baseline from C Prover9."""

    problem: str
    theorem_proved: bool
    clauses_given: int
    clauses_generated: int
    clauses_kept: int
    proof_length: int
    user_cpu_seconds: float
    megabytes: float
    demod_attempts: int = 0
    demod_rewrites: int = 0


# Baselines generated on: 2026-04-06
# Prover9 version: 2017-11A (CIIRC)
# Platform: macOS (Apple Silicon)
C_BASELINES: dict[str, CBaseline] = {
    "x2": CBaseline(
        problem="x2",
        theorem_proved=True,
        clauses_given=12,
        clauses_generated=118,
        clauses_kept=23,
        proof_length=16,
        user_cpu_seconds=0.00,
        megabytes=0.06,
        demod_attempts=702,
        demod_rewrites=156,
    ),
    "bench_ring_comm": CBaseline(
        problem="bench_ring_comm",
        theorem_proved=True,
        clauses_given=44,
        clauses_generated=1034,
        clauses_kept=189,
        proof_length=0,  # Will be filled in
        user_cpu_seconds=0.01,
        megabytes=0.29,
    ),
    "bench_robbins": CBaseline(
        problem="bench_robbins",
        theorem_proved=True,
        clauses_given=44,
        clauses_generated=4480,
        clauses_kept=1110,
        proof_length=0,
        user_cpu_seconds=0.05,
        megabytes=3.20,
    ),
    "bench_group_comm_3": CBaseline(
        problem="bench_group_comm_3",
        theorem_proved=True,
        clauses_given=48,
        clauses_generated=5643,
        clauses_kept=1540,
        proof_length=0,
        user_cpu_seconds=0.09,
        megabytes=0.0,
    ),
    "bench_lattice_distrib": CBaseline(
        problem="bench_lattice_distrib",
        theorem_proved=True,
        clauses_given=165,
        clauses_generated=46947,
        clauses_kept=6763,
        proof_length=0,
        user_cpu_seconds=0.39,
        megabytes=14.18,
    ),
    "bench_luka20": CBaseline(
        problem="bench_luka20",
        theorem_proved=True,
        clauses_given=7,
        clauses_generated=18,
        clauses_kept=15,
        proof_length=0,
        user_cpu_seconds=0.00,
        megabytes=0.0,
    ),
    "bench_mv4": CBaseline(
        problem="bench_mv4",
        theorem_proved=True,
        clauses_given=8,
        clauses_generated=42,
        clauses_kept=12,
        proof_length=0,
        user_cpu_seconds=0.00,
        megabytes=0.0,
    ),
    "bench_ternary_boolean": CBaseline(
        problem="bench_ternary_boolean",
        theorem_proved=True,
        clauses_given=6,
        clauses_generated=30,
        clauses_kept=11,
        proof_length=0,
        user_cpu_seconds=0.00,
        megabytes=0.0,
    ),
}


def get_baseline(problem: str) -> CBaseline | None:
    """Get the C baseline for a problem, or None if not available."""
    return C_BASELINES.get(problem)


def get_all_baselines() -> dict[str, CBaseline]:
    """Get all C baselines."""
    return dict(C_BASELINES)
